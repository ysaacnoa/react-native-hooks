import { colorTokens } from './token-colors';

export const colors = {
  // Brand
  primary: colorTokens.purple700,

  // Status
  success: colorTokens.green500,
  danger: colorTokens.red500,

  // Surface / fondos
  surface: colorTokens.white,
  surfaceDark: colorTokens.gray300,

  // Bordes y divisiones
  border: colorTokens.gray300,

  // Texto
  text: colorTokens.black,
  textMuted: colorTokens.gray400,
  textInverse: colorTokens.white,

  // Grises generales
  gray: colorTokens.gray400,
  grayLight: colorTokens.gray300,
  grayDark: colorTokens.gray600,

  white: colorTokens.white,
  black: colorTokens.black,
};
